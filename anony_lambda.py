"""
They are defined by using the lambda keyword.

So when you define anonymous functions you don't use the normal DCF function definition.

Anonymous functions can take several arguments but they can only have the one expression.

"""

a = lambda b: b + 4

print(a(4))
